package ru.team.pickup.persistence;

/**
 * TextStateStore — файловое хранилище, У2
 *
 * Получает через конструктор:
 *
 * Path path;
 * LineCodec<Order> orderCodec;
 * LineCodec<StorageCell> cellCodec;
 *
 * При сохранении:
 *
 * Проверяет снимок.
 * Создаёт нужный каталог.
 * Записывает заголовок с версией формата и количеством записей.
 * Записывает заказы и ячейки через кодеки.
 * Закрывает ресурсы через try-with-resources.
 *
 * При загрузке:
 *
 * Читает заголовок.
 * Проверяет версию и количество записей.
 * Восстанавливает объекты во временные списки.
 * Создаёт AppSnapshot.
 * Проверяет его связи.
 * Возвращает только полностью корректный результат.
 *
 * Репозитории этот класс не заполняет: это сделает Main.
 */
public class TextStateStore implements StateStore {
    // TODO: добавить реализацию в задаче ответственного участника.
}
