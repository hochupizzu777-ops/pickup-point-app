package ru.team.pickup.service;

/**
 * PickupPointService — операции пункта выдачи, У1
 *
 * Получает через конструктор:
 *
 * Repository<Order, String>
 * WarehouseOperations
 * Clock
 * Queue<String>
 *
 * Основные методы:
 *
 * Метод	Требуемое поведение
 * acceptOrder(...)	Проверить данные, занять ячейку, сохранить новый заказ
 * issueOrder(id, code)	Проверить код, статус и срок, освободить ячейку, отметить выдачу
 * returnOrder(id)	Проверить возможность возврата, освободить ячейку, отметить возврат
 * markExpiredOrders()	Отметить просроченные заказы и создать уведомления
 * findOrder(id)	Найти заказ
 * snapshot()	Получить согласованный снимок заказов и ячеек
 *
 * Правила:
 *
 * Новый номер не должен повторяться, включая номера исторических заказов.
 * Начальный статус — READY_FOR_PICKUP.
 * Дата окончания хранения — дата поступления плюс 7 дней.
 * В последний день хранения выдача разрешена.
 * Выдача возможна только из READY_FOR_PICKUP.
 * Возврат возможен из READY_FOR_PICKUP или EXPIRED.
 * Просрочка не освобождает ячейку.
 * Повторная проверка просрочки не создаёт повторные уведомления.
 * Ожидаемый отказ не оставляет частично изменённое состояние.
 *
 * Это главный координатор: он меняет заказ и склад согласованно.
 *
 * При подключении фона все методы доступа к состоянию защищаем одним монитором экземпляра сервиса. Поиск и создание снимка тоже должны использовать эту защиту.
 */
public class PickupPointService {
    // TODO: добавить реализацию в задаче ответственного участника.
}
