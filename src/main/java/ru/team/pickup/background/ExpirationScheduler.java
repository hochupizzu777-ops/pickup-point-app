package ru.team.pickup.background;

/**
 * ExpirationScheduler — фоновая проверка, У2
 *
 * Получает:
 *
 * Runnable task;
 * long intervalSeconds;
 * Queue<String> notifications;
 *
 * Использует ScheduledExecutorService.
 *
 * Методы:
 *
 * void start();
 * void stop() throws InterruptedException;
 *
 * Требования:
 *
 * Запускать одну периодическую задачу.
 * Не допускать повторного запуска второго фонового процесса.
 * Между выполнениями выдерживать заданный интервал.
 * Сообщать об ошибках задачи через очередь.
 * При остановке завершать исполнитель и ожидать окончания текущей работы.
 * Не печатать сообщения поверх пользовательского ввода.
 *
 * Он запускает переданную задачу, но сам не реализует поиск просроченных заказов.
 */
public class ExpirationScheduler {
    // TODO: добавить реализацию в задаче ответственного участника.
}
