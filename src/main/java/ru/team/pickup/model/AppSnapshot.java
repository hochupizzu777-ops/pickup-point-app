package ru.team.pickup.model;

/**
 * AppSnapshot — снимок состояния, У2
 *
 * Поля:
 *
 * List<Order> orders;
 * List<StorageCell> cells;
 *
 * Методы: конструктор, getters, validate().
 *
 * Списки копируются и становятся неизменяемыми.
 *
 * validate() проверяет связи:
 *
 * номера заказов и ячеек не повторяются;
 * указанная в заказе ячейка существует;
 * ячейка подходит по размеру;
 * активный заказ действительно занимает свою ячейку;
 * занятая ячейка ссылается на существующий активный заказ;
 * выданный или возвращённый заказ больше не занимает место.
 *
 * Историческая ячейка выданного заказа может быть занята другим заказом.
 *
 * Снимок нужен для сохранения и отчётов: он представляет согласованное состояние приложения на один момент времени.
 */
public final class AppSnapshot {
    // TODO: добавить реализацию в задаче ответственного участника.
}
